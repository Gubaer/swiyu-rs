// Worker entry for Monaco's JSON language worker (schema validation,
// autocomplete). Bundled separately by esbuild; see editor.worker.ts.
import 'monaco-editor/esm/vs/language/json/json.worker.js';
