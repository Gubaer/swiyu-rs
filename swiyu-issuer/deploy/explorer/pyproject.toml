[project]
name = "swiyu-issuer-deploy-explorer"
version = "0.0.0"
description = "Tooling for the swiyu-issuer explorer deploy artifacts (compose generator)."
requires-python = ">=3.10"
dependencies = [
    "ruamel.yaml>=0.18",
]

[tool.uv]
package = false
